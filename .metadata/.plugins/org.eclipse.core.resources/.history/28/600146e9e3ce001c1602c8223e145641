/*Escribir un programa que mientras que el usuario ingrese un caracter letra
minúscula, pida:

ingresar un numero entero. Si el número ingresado está
entre 1 y 5 inclusive

deberá imprimir la tabla de multiplicar de dicho
numero.*/

import java.io.BufferedReader;
import java.io.InputStreamReader;

public class ejercicio8_5 {	
	public static void main(String[] args)
	{
		//Constantes
		final int MAXMULTIPLICADOR = 10;
		final int MAX = 5;
		final int MIN = 1;
		
		int numero;
		char caracter;
		
		BufferedReader entrada = new BufferedReader(new InputStreamReader(System.in));
		try{
			
		}catch(Exception exc){
			System.out.println(exc);
		}
		
	}

}
